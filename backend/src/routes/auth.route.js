// import { login, register } from '../modules/auth/controller.js';

export default async function (fastify, opts) {
    // fastify.post('/api/auth/login', login);
    // fastify.post('/api/auth/register', register);
}
